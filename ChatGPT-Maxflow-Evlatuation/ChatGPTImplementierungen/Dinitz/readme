v1 = generic version
v2 = Speicher reservieren 
v3 = Layer Skipping 
v4 = Capacity Scaling 
v5 = Dynamic Level Graph Updates (DLGU) 
v6 = v5 + parallele BFS (mit -ltbb kompilieren) benötigt die tbb library


MemoryPools = v4 + Memory Pools + eigenes Memory management (keine Verbesserung, da Einlesezeit nicht mit berücksichtigt wird)
dinitzEdgeSorting = Kanten sortieren in DFS (langsam, auf ChatGPT Empfehlung nicht verwendet)

