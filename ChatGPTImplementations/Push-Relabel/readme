v1 = generic push-relabel
v2 = bucket-list
v3 = bucket-list + global-relabeling
v4 = bucket-list + global-relabeling + gap heuristic
v5 = v4 parallel -> need to compile with -ltbb, has tbb dependency
